import { View, Text, StyleSheet } from "react-native";

const NextDayPlan = () => {
  return (
    <View style={styles.container}>
      <Text>NextDayPlan</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#fff",
    alignItems: "center",
    justifyContent: "center",
  },
});

export default NextDayPlan;
